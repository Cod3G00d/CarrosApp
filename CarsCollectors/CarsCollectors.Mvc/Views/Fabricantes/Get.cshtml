@model IEnumerable<CarsCollectors.Mvc.Models.FabricanteVM>

@{
    ViewBag.Title = "Get";
}

<h2>Get</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AnoFundacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AnoFundacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FabricanteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FabricanteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FabricanteId })
        </td>
    </tr>
}

</table>
